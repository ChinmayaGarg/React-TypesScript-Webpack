import ReactDOM from 'react-dom';
import { App } from './App';

// We then mount the App component onto the div element with id=root using the react-dom library
ReactDOM.render(<App />, document.getElementById('root'));
